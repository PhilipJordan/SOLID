//$(document).ready(function () {

    //This didn't seem to do anything
    //$(".mapImage").each(function (index, element) {
    //    var elementWidth = $(element).width();

    //    element.css({
    //        'height': cw + 'px'
    //    });
    //});


//});